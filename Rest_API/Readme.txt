This script requests specific numeric data through an API, which is then stored in the PRTG result set. To execute the script, you can add parameters as follows:

-Username "YourUsername" -Password "YourPassword"
